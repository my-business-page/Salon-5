document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const loader=$('#loader');document.body.classList.add('is-loading');if(loader&&!reduce){setTimeout(()=>{loader.classList.add('is-hidden');document.body.classList.remove('is-loading');setTimeout(()=>loader.remove(),850)},900)}else{loader?.remove();document.body.classList.remove('is-loading')}const progress=document.createElement('div');progress.id='scroll-progress';document.body.append(progress);const glow=document.createElement('div');glow.className='cursor-glow';document.body.append(glow);const onScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max?scrollY/max*100:0)+'%';$('header')?.classList.toggle('scrolled',scrollY>35)};addEventListener('scroll',onScroll,{passive:true});onScroll();if(!reduce&&matchMedia('(hover:hover)').matches){let x=0,y=0,tx=0,ty=0;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY;glow.style.opacity='.8'});const loop=()=>{x+=(tx-x)*.09;y+=(ty-y)*.09;glow.style.left=x+'px';glow.style.top=y+'px';requestAnimationFrame(loop)};loop()}const io=new IntersectionObserver(entries=>entries.forEach(e=>{e.target.classList.toggle('visible',e.isIntersecting);e.target.classList.toggle('exit',!e.isIntersecting&&e.boundingClientRect.top<0)}),{threshold:.1,rootMargin:'-8% 0px -8% 0px'});$$('.reveal').forEach((el,i)=>{el.style.transitionDelay=(i%5)*65+'ms';io.observe(el)});if(!reduce&&matchMedia('(hover:hover)').matches){$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${py*-4}deg) rotateY(${px*4}deg) translateY(-9px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});$$('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')})}$$('a[href*="wa.me"]').forEach(a=>a.setAttribute('aria-label','Book via WhatsApp'));$$('a[href^="tel:"]').forEach(a=>a.setAttribute('aria-label','Call The Living Room Hair Studio'))});